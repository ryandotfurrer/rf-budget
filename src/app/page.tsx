import Image from "next/image";

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between p-24">
      <header>
        <h1 className="text-4xl font-bold">Welcome to RF Budget</h1>
        <p>
          Budgeting shouldn&apos;t need to be complicated, RF Budget fixes that
        </p>
      </header>
    </main>
  );
}
